════════════════════════════════════════════════════════════════════════════
 80 · EL BOTÓN DE SLAM, DE EXTREMO A EXTREMO
      Funciona. Y destapó tres defectos, uno de ellos por tercera vez.
════════════════════════════════════════════════════════════════════════════
Fecha:  2026-08-07
Robot:  rvr-01


1 · LA CADENA COMPLETA, VERIFICADA
────────────────────────────────────────────────────────────────────────────
    /pedir_slam data:true
      → success=True, «petición ACEPTADA, no arrancado todavía»
      → polkit autoriza (sin sudo)
      → systemd arranca atriz-slam.service
      → ExecStartPre: exclusión OK, barrido ON
      → slam.launch.py, ciclo de vida a active
      → el supervisor pasa a FUNCIONANDO

**Y NO por lo que dijo el supervisor.** Cinco testigos independientes:

    proceso async_slam_tool     1
    unidad systemd              active / success
    barrido                     ENCENDIDO (/scan publica)
    ciclo de vida               active      (no `unconfigured`)
    TF map -> odom              publicando

Y el paro, también por el servicio: unidad `inactive/success`, proceso 0.


2 · M-F · LA REGLA DE POLKIT, EN LAS DOS DIRECCIONES
────────────────────────────────────────────────────────────────────────────
    atriz-slam.service    inactive   rc=0   ← autorizada
    atriz-nav.service     inactive   rc=0   ← autorizada
    apt-daily.service     inactive   rc=1   Interactive authentication required
    man-db.service        inactive   rc=1   Interactive authentication required
    systemd-tmpfiles…     inactive   rc=1   Interactive authentication required
    ssh.service           active     rc=1   Interactive authentication required

🔴 **El confusor se descartó ANTES de cantar el resultado.** La primera prueba
   comparó `atriz-slam` (inactiva) con `ssh` (activa), así que el rc=0 podía
   deberse a que systemd ni consulta a polkit para parar algo ya parado. Las
   tres unidades **inactivas y fuera de la regla** piden autenticación igual:
   el rc=0 es la regla, no el estado.


3 · TRES DEFECTOS QUE DESTAPÓ LA PRUEBA
────────────────────────────────────────────────────────────────────────────

a) 🔴 EL SUPERVISOR Y LA UNIDAD MIRABAN MAPAS DISTINTOS

     supervisor:    …/install/atriz_rvr_bringup/share/…/maps/aula.yaml
     atriz-nav.sh:  …/src/Atriz_rvr/atriz_rvr_bringup/maps/aula.yaml

   `PathJoinSubstitution([bringup, …])` resuelve al directorio INSTALADO; el
   script usa el FUENTE. Es exactamente lo que el comentario que yo mismo
   escribí decía evitar: *«si no coinciden, el supervisor diría "hay mapa" y la
   unidad fallaría por no encontrarlo»* — y el caso peor es el inverso: el botón
   deshabilitado sobre un mapa que sí está.
   → El launch ya no impone ruta; el nodo usa el mismo defecto que el script y
     lee `ATRIZ_MAPA`.

b) ⚠️ LOS RECHAZOS DABAN UN SOLO MOTIVO

   `/pedir_nav` con SLAM corriendo Y sin mapa contestaba solo «no hay mapa».
   Quien lo leyera iría a hacer un mapa, volvería, y se estrellaría contra la
   exclusión: **dos viajes para un problema que se podía contar de una vez.**
   → Ahora devuelve todos los motivos separados por «·».

c) 🔴 EL BARRIDO SE QUEDABA ENCENDIDO, Y ES LA TERCERA VEZ DEL MISMO PATRÓN

   Tras parar SLAM, `/scan` seguía publicando y **el journal no tenía ni una
   línea** del `ExecStopPost`. Causa:

       systemctl is-active atriz-slam   →  imprime "inactive"  y devuelve 3

   Con `set -euo pipefail`, esa asignación mata el guion **antes de decidir
   nada**. Y con el `-` del `ExecStopPost`, systemd se lo traga en silencio.

   🔴 **Es la tercera vez que este proyecto tropieza con la misma familia:**

       `(( t++ ))` con t=0 devuelve 1        → desactivó la espera de puertos
                                                escrita para evitar el fallo
                                                que luego ocurrió
       `[[ … ]] && kill`                     → abortaba un banco de medición
       `EST=$(systemctl is-active …)`        → dejaba el barrido encendido

   **Un comando cuyo código no-cero es una RESPUESTA, no un error.** Con
   `set -e` los tres mueren sin decir nada.

   → Arreglado con `|| true` y un `if` explícito. Verificado en las dos ramas:
     apaga cuando no queda nadie (`rc=0`, y el barrido queda apagado de verdad),
     y la rama «se deja encendido» dispara con una unidad activa.


4 · LO QUE SIGUE SIN VERIFICAR
────────────────────────────────────────────────────────────────────────────
⏳ **El botón de Nav2 de extremo a extremo.** No hay mapa en la ruta que ambos
   miran (`maps/aula.yaml`); el del cuarto está en `~/mapas/cuarto.yaml`. Los
   rechazos sí están verificados; el arranque no.
⏳ **`off-si-sobra` con DOS unidades activas a la vez.** La rama está verificada
   por lógica (con `atriz-robot` como unidad activa), no de extremo a extremo:
   para eso hacen falta SLAM y Nav2 arriba, y Nav2 necesita mapa.
⏳ **`PartOf=` con carga real**: medido con unidades de juguete (evidencia 78),
   no con un `slam_toolbox` de verdad.


════════════════════════════════════════════════════════════════════════════
 AMPLIACIÓN · EL BOTÓN DE NAV2, TAMBIÉN DE EXTREMO A EXTREMO (2026-08-07)
════════════════════════════════════════════════════════════════════════════

5 · EL `EnvironmentFile` SE LEE DE VERDAD — con prueba que DISCRIMINA
────────────────────────────────────────────────────────────────────────────
La primera comprobación no valía: el valor de `/etc/default/atriz` era
IDÉNTICO al defecto del nodo, así que el `param get` habría salido igual sin
leer el fichero. **Un instrumento que no distingue las dos hipótesis.**

Se cambió el fichero a un valor distinto y se reinició:

    /etc/default/atriz   ATRIZ_MAPA=/home/sphero/mapas/cuarto.yaml
    param del nodo       /home/sphero/mapas/cuarto.yaml     ← CAMBIÓ
    hay_mapa             true

✅ El fichero se lee. Y de paso quedó apuntando al mapa del cuarto, que es lo
   que permitió la prueba siguiente.


6 · NAV2, DE EXTREMO A EXTREMO
────────────────────────────────────────────────────────────────────────────
    /pedir_nav data:true  →  success=True, «petición ACEPTADA»

    estado      0,7 s  CIEGO        ← 🔴 defecto, ver §8
                1,4 s  ARRANCANDO
                3,4 s  FUNCIONANDO

Y los testigos independientes del supervisor:

    unidad                  active / success
    barrido                 ENCENDIDO (/scan publica)
    bt_navigator            active        planner_server   active
    controller_server       active        map_server       active
    amcl                    active
    ¿acepta objetivos?      SÍ   (cliente de acción, no `ros2 action list`)

Y el paro, también por el servicio: `inactive/success`, `amcl` fuera.

⚠️ **No se envió ningún objetivo: el robot no se movió en ningún momento.** Que
   Nav2 NAVEGUE sigue sin probarse.


7 · LA LATENCIA DEL SERVICIO — y el instrumento que volvió a mentir
────────────────────────────────────────────────────────────────────────────
🔴 `ros2 service call` dio **6,3 s**, por encima de los 5,0 s de la cadena web.
   **No es la latencia del servicio: es el arranque del CLI**, que crea un nodo
   y descubre. Este proyecto ya se equivocó exactamente así con `/start_scan`
   en julio, y lo tuvo que retractar.

Medido con un cliente PERSISTENTE, o sea con la conexión ya abierta —que es lo
que hace la web—:

    /pedir_nav data:true    1,021 s
    /pedir_nav data:true    0,957 s
    /pedir_nav data:false   0,053 s

✅ **5× de margen** sobre los 5,0 s. El arranque cuesta ~1 s por los rechazos
   (dos consultas a systemd, el mapa y `ps`); el paro no los hace y cuesta 0,05.


8 · UN DEFECTO QUE SOLO APARECE AL EJECUTARLO: `CIEGO` EN UN ARRANQUE SANO
────────────────────────────────────────────────────────────────────────────
La máquina de estados comprobaba `CIEGO` **antes** que `ARRANCANDO`, así que un
arranque perfectamente normal pasaba ~1 s por:

    «encendido pero SIN barrido: no puede funcionar»

Los nodos ya existen y `/scan` todavía no ha llegado. **La web habría pintado
una alarma naranja en mitad de un arranque sano**, y el alumno habría ido a
buscar una avería que no existe.

→ `ARRANCANDO` va ahora primero. `CIEGO` significa **«está levantado y NO puede
  funcionar»**, no «todavía está subiendo» — típicamente alguien llamó a
  `/stop_scan` con la navegación en marcha.

📌 No se habría visto leyendo el código: hace falta arrancar Nav2 de verdad y
   mirar la secuencia.


9 · EL `off-si-sobra`, AHORA SÍ BAJO systemd
────────────────────────────────────────────────────────────────────────────
    13:10:32  ✅ escaneo ENCENDIDO — /scan publica, el robot puede conducir
    13:12:46  ✅ escaneo APAGADO — ninguna unidad de navegación lo necesitaba

Las dos líneas en el journal de la unidad. Antes del arreglo del `set -e` no
aparecía **ninguna** y el barrido se quedaba encendido.


10 · LO QUE SIGUE SIN VERIFICARSE, Y UNO NO PODRÁ
────────────────────────────────────────────────────────────────────────────
⏳ **Que Nav2 NAVEGUE.** Arranca y acepta objetivos; nadie le ha mandado uno.
⏳ **`PartOf=` con carga real** (medido con unidades de juguete, evidencia 78).
🔴 **`off-si-sobra` con DOS unidades activas a la vez: NO SE PUEDE PROBAR.**
   SLAM y Nav2 son **excluyentes por diseño** —el `ExecStartPre` de exclusión lo
   impide—, así que la rama «se deja encendido» solo puede verificarse por
   lógica, y así está. **Es honesto decir que existe una comprobación para un
   caso que el diseño actual no permite alcanzar**: la dejamos porque el diseño
   podría cambiar, no porque se haya visto disparar.


11 · EL ARREGLO DEL `CIEGO`, VERIFICADO — y un número que no cuadraba
────────────────────────────────────────────────────────────────────────────
Con el observador suscrito ANTES de pedir nada, para ver la secuencia entera:

     0,7 s   nav=ARRANCANDO
    30,2 s   nav=FUNCIONANDO

✅ **Sin pasar por CIEGO.** El arreglo funciona: `APAGADO → ARRANCANDO →
   FUNCIONANDO`, que es lo que la web debe pintar en un arranque sano.

📊 Y la latencia de la petición, con cliente persistente: **0,223 s**.

⚠️ **PERO 30,2 s NO ES EL 24,3 s DE B2, y hay que decirlo.** No se contradicen:
   miden cosas distintas.

     B2 (n=2)            hasta que /navigate_to_pose ACEPTA OBJETIVOS   24,3 s
     el supervisor (n=1) hasta FUNCIONANDO                              30,2 s

   El supervisor exige **más**: además del servidor de acción, el proceso vivo y
   `/scan` fresco — y sondea a 1 Hz.

   🔴 **El número que verá el alumno es el del supervisor.** El plazo que se le
      había dado al PC (~26 s) se queda corto y se corrige a ~30 s.
   📝 Es **n=1**. Queda muy por debajo del tope duro de 120 s, así que no cambia
      el diseño; solo el texto que pinta la web.
