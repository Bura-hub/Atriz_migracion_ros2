================================================================================
98 · rvr-02, DE LA TARJETA EN BLANCO — el paso a paso tal como ocurrió
================================================================================
Fecha:    2026-08-11
Robot:    rvr-02 (segunda unidad; rvr-01 sigue montado y sin tocar)
PC:       Windows + WSL2 (Ubuntu 26.04)
Estado:   ✅ tarjeta · ✅ arranque y UART · ✅ provision.sh ENTERO (96 ✓ · 0 ✗)
          🟡 verificador: 145 ✓ · 7 avisos · 4 fallos, tres de ellos el mismo
             hueco (grupos del usuario) ya arreglado en el guion
          ⏳ pendiente: repetir el verificador · red.txt (paso 6-bis)

POR QUÉ EXISTE ESTE FICHERO
--------------------------------------------------------------------------------
El 2026-08-10 rvr-02 ya se había aprovisionado a medias y se quedó parado en un
`Permission denied: 'log'`. 👤 El usuario formateó la microSD entera para
rehacerlo desde cero **documentando el proceso**, en vez de arrastrar un estado
a medias que nadie sabría reproducir.

La regla que gobierna esta pasada, escrita por el PC el 2026-08-10:

    «Cada cosa que frene a rvr-02 es una que no frenará a los catorce
     siguientes — si acaba en el guion en vez de arreglarse a mano.»

Por eso abajo no está solo lo que funcionó: está lo que falló, y dónde acabó
arreglado.

================================================================================
LO QUE SE HIZO, EN ORDEN
================================================================================

PASO 1 · Grabar Ubuntu Server 24.04.4 LTS arm64 con Raspberry Pi Imager    👤 PC
--------------------------------------------------------------------------------
Ajustes: hostname `rvr-02`, usuario `sphero`, WiFi (SSID + PSK), y SSH.

🔴 AGUJERO ENCONTRADO AQUÍ, y lo encontró el usuario, no el repositorio:

    «quiero que aclares que la autenticacion de ssh sea por password no por
     public key eso falto»

Tenía razón. Los CUATRO sitios que describen el Imager —`FLOTA.md`,
`MANUAL_ATRIZ_ROS2.md` §3.2, `INSTALACION.md` B1 y `PLAN_MIGRACION_ROS2.md`—
decían «activar SSH» y nada más.

No es un matiz. El Pi va headless y `preparar_tarjeta.sh` le quita además la
consola serie (paso 1/5). Con «solo clave pública» y una clave que no sea la del
PC de acceso: sin teclado, sin pantalla, sin consola y sin SSH. Se regraba.

Medido en rvr-01 ese mismo día, que es lo que fija el criterio de la flota:

    /etc/ssh/sshd_config:   #PasswordAuthentication yes   ← comentado = yes
    ~/.ssh/authorized_keys: existe, 0 bytes, 0 claves

→ a rvr-01 SOLO se entra por contraseña, porque no tiene ninguna clave.

Arreglado en: los cuatro documentos + `preparar_tarjeta.sh` (paso 4/5 nuevo, que
aborta) + `verificar_robot.sh` (falla si `PasswordAuthentication no`).

📌 Y para la imagen dorada: las claves de HOST se regeneran en el primer
   arranque; `~/.ssh/authorized_keys` NO — se clona tal cual. Una clave en el
   robot de referencia abriría los 16.


PASO 1-bis · WSL, porque el PC del laboratorio es Windows                  👤 PC
--------------------------------------------------------------------------------
`preparar_tarjeta.sh` es un guion de Linux y `FLOTA.md` solo contemplaba Linux.
Dos zancadillas reales, las dos ya en `FLOTA.md` §«Windows: preparar la tarjeta
desde WSL»:

🔴 1 · Docker Desktop secuestra `wsl`. Su distro `docker-desktop` era la
       predeterminada. Se reconoce en el acto:

           LAPTOP-XXXX:/mnt/host/c/Users/burav# sudo apt update
           -sh: sudo: not found

       prompt `#` (ya eres root), `-sh` en vez de bash, `sudo` inexistente y
       `/mnt/host/c/` en vez de `/mnt/c/`. No se trabaja ahí: Docker la recrea
       en cada actualización. Se arregla con `wsl --set-default Ubuntu`.

🔴 2 · `wsl --install -d Ubuntu` falla con ERROR_ALREADY_EXISTS si Ubuntu ya
       está instalado aunque figure `Stopped`. No hay que instalarlo: hay que
       arrancarlo. Por eso `wsl --list --verbose` va ANTES.

Salió Ubuntu 26.04 en WSL. Da igual: es el WSL del PC, no el robot.


PASO 1-ter · El PAT que no hacía falta                                    👤 PC
--------------------------------------------------------------------------------
`git clone` del repositorio funcionó SIN pedir credenciales. Comprobado:

    Atriz_migracion_ros2 → PÚBLICO (clona sin credencial)
    Atriz_rvr            → PÚBLICO
    atriz-lab            → privado

👤 El usuario los puso en público a propósito: «lo he vuelto publico para evitar
problemas de credentials». Consecuencias, las dos:

  ✅ DECAE el bloqueante nº 1 de la Fase 6 — `~/.git-credentials` con un token
     personal ya no tiene que viajar en 16 microSD. Retirado del paso 5 de
     `FLOTA.md`, que lo pedía explícitamente.

  🔴 NO arregla, y sigue abierto (👤 usuario): las credenciales que YA están en
     el historial de `Atriz_rvr` — PSK del WiFi y contraseña de `sphero`,
     documentado desde el 2026-08-02 en `API_LABORATORIO.md` §«en público».
     Al ser público el repositorio, ese historial lo lee cualquiera.

⚠️ Clonar DENTRO de WSL, no usar la copia de Windows por `/mnt/c`: git en
   Windows convierte a CRLF y bash muere con `$'\r': command not found`.
   Control: `bash -n scripts/preparar_tarjeta.sh` → «SCRIPT OK, sin CRLF».


PASO 2 · preparar_tarjeta.sh, con la tarjeta en el PC                     👤 PC
--------------------------------------------------------------------------------
WSL2 NO automonta las unidades extraíbles:

    sudo mkdir -p /mnt/e && sudo mount -t drvfs E: /mnt/e

Comprobación del SSH antes de nada (⚠️ `user-data` lleva el hash de la
contraseña y la PSK: se mira la clave, no se vuelca el fichero):

    $ grep -o 'ssh_pwauth: [a-zA-Z]' /mnt/e/user-data
    ssh_pwauth: t                                   ← true

--------------------------------------------------------------------------------
🔴 FALLO ENCONTRADO EN LA PRIMERA PASADA EN SECO — el informe mentía
--------------------------------------------------------------------------------
    ▶ 1/5 · cmdline.txt — liberar el puerto serie
      ✓ quitado console=serial* (reservaba el UART para la consola del kernel)
      contenido: console=serial0,115200 multipath=off ... console=tty1 ...
                 ^^^^^^^^^^^^^^^^^^^^^ SIGUE AHÍ

Dice que lo quitó y lo enseña puesto. Causa, en el código:

    línea 148   con --simular se salta la edición ......... correcto
    línea 154   pero el ✓ se imprime IGUAL, y en pasado ... incorrecto
    línea 188   y el `contenido:` hace cat del fichero real (el de antes)

O sea que el único modo cuyo propósito es VER EL EFECTO por adelantado
afirmaba una acción no ejecutada y enseñaba el estado anterior. Los pasos 2/5 y
3/5 tenían el mismo patrón.

La tarjeta estaba bien. El que mentía era el instrumento — que es exactamente
lo que la regla del proyecto «comprueba el efecto, no el código de salida»
existe para cazar, aplicada esta vez al propio guion.

ARREGLADO (commit 97b94a3):
  · ayudante `hecho()`: «SE HARÍA → quitar console=serial*» en simulación,
    «HECHO → quitar console=serial*» en pasada real. Mismo texto, prefijos que
    no se pueden confundir.
  · la línea resultante se calcula SIEMPRE y se escribe UNA sola vez al final
    del paso, así el ensayo puede enseñar «contenido que QUEDARÍA».
  · DOS controles del efecto que no existían:
      - aborta si `console=serial` sobrevive a la edición
      - aborta si una clave queda escrita en config.txt pero NO activa para
        [pi4] (la trampa de la cabecera [all], que no da error al faltar)

Verificado contra una RÉPLICA EXACTA de esta tarjeta (su cmdline.txt con el
`ds=nocloud;i=rpi-imager-` y el `regdom=CO`, y un config.txt terminado en [cm4]):
  · --simular  → `diff -r` contra la copia: ficheros IDÉNTICOS, no escribe nada
  · real       → 0 ocurrencias de console=serial, 0 saltos de línea,
                 disable-bt ACTIVO en [all], ROBOT_ID=02, los dos .bak-* creados
  · 2 pasadas  → 1 sola directiva y 1 solo bloque añadido

⚠️ LECCIÓN DE MEDICIÓN: la idempotencia PARECIÓ rota porque
   `grep -c 'disable-bt'` dio 2. Contaba también la línea de COMENTARIO que
   explica qué hace disable-bt. El guion estaba bien; el instrumento, no.
   Cuéntese `^[[:space:]]*dtoverlay=disable-bt`. Segunda vez en la misma sesión
   que el medidor falla y no lo medido.

--------------------------------------------------------------------------------
🔴 Y una afirmación FALSA del propio guion, refutada por esta tarjeta
--------------------------------------------------------------------------------
El comentario del bloque `regdom` decía que el `cfg80211.ieee80211_regdom=CO` de
rvr-01 «se puso a mano y no quedó registrado» porque «NINGÚN script lo escribía».

La tarjeta de rvr-02, recién grabada y sin arrancar nunca, YA lo traía, junto al
marcador `ds=nocloud;i=rpi-imager-1786472033701`. **Lo escribe el Raspberry Pi
Imager** al fijar el país del WiFi. El bloque del guion es una red de seguridad
para quien grabe sin poner el país, no el origen del valor.

--------------------------------------------------------------------------------
PASADA DEFINITIVA — el resultado, medido sobre la tarjeta
--------------------------------------------------------------------------------
    ▶ 1/5   ✓ respaldo: cmdline.txt.bak-20260811-135546
            ✓ HECHO → quitar console=serial*
            ✓ cmdline.txt ya pide regdom=CO
    ▶ 2/5   ✓ enable_uart=1 ya está activo (24.04 lo trae por defecto)
            ✓ respaldo: config.txt.bak-20260811-135546
            ✓ HECHO → añadir bajo [all]: dtoverlay=disable-bt
    ▶ 3/5   ✓ HECHO → escribir ROBOT_ID=02 → rvr-02 · ROS_DOMAIN_ID=2 · /rvr_02
    ▶ 4/5   ✓ ssh_pwauth activo
    ▶ 5/5   ✓ sync completado

    $ grep -c 'console=serial' /mnt/e/cmdline.txt     → 0
    $ wc -l < /mnt/e/cmdline.txt                      → 0

✅ CIERRA UNA DUDA QUE ESTABA ABIERTA: los respaldos `cp -a` SÍ funcionan sobre
   `drvfs`. El guion solo imprime «✓ respaldo:» si el `cp -a` devolvió 0, y los
   imprimió los dos. Importaba porque el guion no lleva `set -e`: si `cp -a`
   hubiera fallado, no habría parado y nos habríamos quedado sin respaldo sin
   enterarnos.

📌 `preparar_tarjeta.sh` pasa de «hace tres cosas» a CUATRO (se le añadió la
   comprobación del SSH), y su mensaje final ahora recuerda `red.txt`, que es el
   paso 3 de FLOTA.md y que el guion no escribe.


PASO 3 · red.txt — APLAZADO A PROPÓSITO                                   ⏳ 👤
--------------------------------------------------------------------------------
👤 Decisión del usuario, tomada con la tarjeta aún montada.

Motivo: la tabla de asignación de `FLOTA.md` tiene la IP de laboratorio de
rvr-02 **en blanco** —el administrador de red las asigna una a una— y la propia
plantilla avisa de que una IP estática equivocada te deja fuera del robot.

Es legítimo aplazarlo: sin `red.txt`, `atriz-first-boot` NO adivina. Deja la red
como esté (el DHCP que configuró el Imager), lo registra en
`/var/log/atriz-first-boot.log` y lo reintenta al siguiente arranque. El robot
arranca y se llega por `rvr-02.local`.

🔴 PERO deja de poder aplazarse en el paso 6-bis (`first-boot.sh --solo-red`),
   que no tendría de dónde leer la IP. Añadido ahí cómo crearlo desde el propio
   robot. Sin ese paso, rvr-02 se queda en DHCP puro y NO está desplegado.

================================================================================
LO QUE QUEDA, Y LO QUE HAY QUE VIGILAR
================================================================================

⏳ PASO 4 · Primer arranque y SSH. Control obligatorio nada más entrar:

       cat /proc/device-tree/soc/serial@7e215040/status   # debe: disabled
       cat /proc/device-tree/aliases/serial0              # /soc/serial@7e201000

   🔴 Si el mini-UART no dice `disabled`, el paso 2 no se aplicó: PARAR.
   ⚠️ Es la comprobación CORREGIDA. La primera versión miraba `aliases/uart0`
      —que da lo mismo siempre, no podía fallar nunca— y `ls /dev/serial0`, que
      no existe en Ubuntu (es de Raspberry Pi OS).

⏳ PASO 5 · Clonar. Ya SIN credenciales (ver paso 1-ter).

✅ PASO 6 · `provision.sh` — EJECUTADO ENTERO POR PRIMERA VEZ EN LA HISTORIA
--------------------------------------------------------------------------------
Era «la suposición más peligrosa que le queda al proyecto»: hasta hoy solo se
había probado con `--simular`, que convierte en no-operación justo lo que
instala. Se acabó.

PRIMERA PASADA (~40 min, 9.075 líneas de registro) → 2 pasos caídos:

    ✗ colcon build falló
    ✗ 2 comprobaciones fallaron. No se instala nada.
    ✗ fase_7_systemd.sh falló (¿falta /boot/firmware/robot_id.txt?)
    PASOS CON PROBLEMAS: colcon build · arranque automático

Y es EL MISMO fallo del 2026-08-10. O sea reproducible, que es exactamente para
lo que servía tener un segundo robot.

🔴 LA CAUSA — un `install -d`, y estaba escondida a plena vista
--------------------------------------------------------------------------------
    drwxr-xr-x root:root  ~/atriz_ws        ← el padre
    drwxr-xr-x sphero     ~/atriz_ws/src    ← el hijo

`provision.sh:244` hacía:

    install -d -o "$USUARIO" -g "$USUARIO" "$HOME_USUARIO/atriz_ws/src"

Parece correcto: nombra al usuario y crea el árbol. No lo es. El manual de
coreutils de la propia máquina lo dice sin ambigüedad:

    «Parent directories are created with mode u=rwx,go=rx (755), regardless of
     the -m option» … «giving them the DEFAULT attributes»

`install -d` NO aplica `-o`/`-g` a los padres que crea de paso. Y como el guion
corre con `sudo`, «por defecto» es **root**. Así que `src` salía del usuario y
`atriz_ws` de root.

Después `colcon build` va como el usuario (`sudo -u`) y no puede crear `build/`,
`install/` ni `log/` dentro de un 755 ajeno → `Permission denied: 'log'`. Y de
rebote `fase_7_systemd.sh` se niega porque el workspace no está compilado, así
que el robot se queda sin arranque automático.

**Dos de los nueve pasos caídos por el dueño de un directorio.**

⚠️ UN RAZONAMIENTO MÍO QUE HUBO QUE RETIRAR: al ver `atriz_ws` con fecha 14:35 y
   `src` con 14:51 concluí que «no los creó la misma llamada, algo crea atriz_ws
   antes del paso 5». Falso: `mtime` NO es fecha de creación. El 14:35 de
   `atriz_ws` es cuando se creó `src` dentro, y el 14:51 de `src` es cuando se
   clonó el ydlidar dentro. Los dos son compatibles con una sola llamada, que es
   lo que resultó ser. Se comprobó leyendo el manual, no deduciendo de fechas.

ARREGLOS, los cuatro (commit 8dc0361):
  1. Se nombran los DOS directorios en el `install -d`. Nombrar el padre no es
     redundante: es la ÚNICA forma de que reciba los atributos pedidos.
  2. Reparación de lo ya creado (`chown -R` si el dueño no es el usuario). Sin
     esto, ser «idempotente» no serviría de nada aquí: la primera víctima del
     fallo es un robot que ya existe y al que hay que relanzarle el guion.
  3. `colcon build` deja de mandar su salida a `/dev/null`. En la primera
     ejecución completa del guion en la historia del proyecto, el ÚNICO paso que
     falló fue justo ése — y había tirado su propia evidencia. 9.075 líneas para
     decir «✗ colcon build falló» y nada más; la causa hubo que sacarla mirando
     el dueño de un directorio a mano. Ahora va a `~/atriz_ws/colcon-build.log`.
  4. `verificar_robot.sh` comprueba el dueño de `atriz_ws` y sus cuatro
     subdirectorios. **No lo vigilaba nadie.** Verificado en rvr-01, que pasa.

SEGUNDA PASADA, ya con el arreglo:

    ! ~/atriz_ws es de 'root', no de 'sphero': colcon build no podría escribir ahí
    ✓ ~/atriz_ws devuelto a sphero (era el fallo de rvr-02, 2026-08-10)
    ...
    ✓ workspace compilado (colcon build)
    ✓ fase_7_systemd.sh completado: el robot arrancará solo

    RECUENTO: 96 ✓ · 16 avisos · 0 fallos
    Y el 9/9 sin la línea «PASOS CON PROBLEMAS».

Los 16 avisos son todos esperados y marcados «a propósito» en el propio guion:
reinicio pendiente del kernel, snapd en disco, el gobernador, y
`atriz-nav`/`atriz-slam` instalados pero NO habilitados (se piden desde la web).

✅ Y OTRO ⏳ QUE SE CIERRA, DE PROPINA: EL ID_PATH DEL LIDAR
--------------------------------------------------------------------------------
    ✓ /dev/ydlidar existe: la regla CASA en este robot

La regla udev lleva a fuego el `ID_PATH` del puerto USB de rvr-01, y estaba
escrito en `FLOTA.md` como ⏳ NO VERIFICADO que fuese el mismo en otro Pi — con
la advertencia de que si no coincidía, no habría `/dev/ydlidar` → sin `/scan` →
el `collision_monitor` bloquea el movimiento → **el robot parecería averiado**.

**Coincide.** Medido en rvr-02:  /dev/rvr → ttyAMA0  ·  /dev/ydlidar → ttyUSB0

Era la última incógnita grande antes de la imagen dorada. No hay que tocar la
regla udev por robot.

⏳ PASO 6-bis · `red.txt` + `first-boot.sh --solo-red` (ver paso 3).

✅ PASO 6-ter · EL LIDAR — cerrado, ver arriba. La regla es clonable tal cual.

🟡 PASO 7 · `verificar_robot.sh --hardware`  →  145 ✓ · 7 avisos · 4 FALLOS
--------------------------------------------------------------------------------
🔴 TRES DE LOS CUATRO FALLOS ERAN EL MISMO HUECO: LOS GRUPOS DEL USUARIO

    ✗ /dev/rvr sin permisos para sphero
    ✗ el RVR NO contesta
    ✗ throttling / under-voltage: «Can't open device file: /dev/vcio»

**Ningún guion del proyecto metía al usuario en `dialout` ni en `video`.** En
rvr-01 los tiene —`groups=…,20(dialout),…,44(video),…`— pero de su montaje
MANUAL original, no de un script.

⚠️ Y NO SE HABRÍA VISTO NUNCA. La imagen dorada clona `/etc/group`, así que los
   robots 3..16 los heredarían de rvr-01 y todo parecería bien. Es exactamente
   el peligro que el proyecto ya tenía escrito: «la imagen es el ATAJO, el
   script es la VERDAD. Si divergen, gana el script». Aquí divergían, y solo una
   instalación LIMPIA podía enseñarlo.

POR QUÉ NO SALTÓ ANTES, Y POR QUÉ IMPORTA MÁS DE LO QUE PARECE:
`atriz-robot.service` lleva `SupplementaryGroups=dialout`, así que EL SERVICIO
habla con el RVR aunque el usuario no esté en el grupo — de ahí que el mismo
verificador diera `✓ /odom a 15.32 Hz` y `✓ batería 98 %` dos secciones antes de
decir «el RVR NO contesta». No es una contradicción: son dos caminos distintos
al mismo cable.

Lo que se rompe es todo lo INTERACTIVO. Y eso incluye
`scripts/estudiantes/atriz.py`, **que es el producto que ejecuta el alumno**.

ARREGLO: `provision.sh` mete al usuario en `dialout` y `video` (paso 3/9, junto
al UART), es idempotente y avisa de que no surte efecto hasta reiniciar.

  · dialout → /dev/rvr (ttyAMA0) y el /dev/ttyUSB0 del LIDAR
  · video   → /dev/vcio, que es como vcgencmd lee las bajadas de tensión

⏳ EL CUARTO FALLO, SIN DIAGNOSTICAR: `✗ servicios que NO responden: get_encoders`
   El topic `/encoders` SÍ va (136 mensajes en 8 s, ~17 Hz), así que el driver
   lee los encoders; lo que no contestó es el SERVICIO. Hipótesis sin comprobar:
   que el intento fallido de hablar con el RVR justo antes (fallo 2) dejara el
   enlace ocupado. **No se da por diagnosticado**: se repite el verificador con
   los grupos ya aplicados y se mira si sigue.

🔴 Y DOS FALLOS DEL PROPIO VERIFICADOR, LOS DOS MÍOS Y DE HOY:

  1. `verificar_robot.sh: line 1910: [[: 0\n0: syntax error`
     La comprobación de `authorized_keys` que escribí esta mañana hacía
     `grep -c … || echo 0`. Con el fichero VACÍO, `grep -c` imprime «0» **y sale
     con código 1** (no casó nada), así que el `|| echo 0` añadía un segundo cero
     y la variable quedaba en «0\n0». `grep -c` siempre imprime un número: el
     respaldo solo hace falta si el fichero no existe.

  2. Un aviso que contradecía a otra comprobación del mismo guion:
         ✓ Aproximacion.radius = 0.15 (el valor decidido el 2026-08-09)
         ! el radius del collision_monitor no es 0.18
     Una mira el parámetro del NODO VIVO y exige 0.15; la otra miraba el FICHERO
     y seguía pidiendo 0.18, el valor antiguo. Las dos comprobaciones son
     legítimas —fichero y nodo no son lo mismo— pero tienen que pedir LO MISMO.
     Alineada: 0.15 ✓ · 0.18 FALLO · otra cosa aviso.

     📌 Segunda vez en el mismo día que aparece este patrón: por la mañana fue
        `authorized_keys`, con dos comprobaciones de polaridad contraria. El
        verificador tiene 145 comprobaciones y nada impide que dos se
        contradigan.

AVISOS (7), todos esperados o ya conocidos:
  · sin identidad de git global (es del usuario, no del robot)
  · no hay 60-atriz.yaml ni .network por SSID  → es el paso 6-bis, aplazado
  · ATRIZ_MAPA apunta a un mapa que no existe  → normal: aún no se ha mapeado
  · mDNS por enlace en wlan0                   → pendiente conocido
  · authorized_keys vacío                      → correcto para la imagen dorada

✅ LO QUE SÍ PASÓ, y no es poco: UART y PL011, ROS 2 Jazzy con 276 paquetes,
   `/odom` a 15.32 Hz, batería 98 %, el LIDAR dando 99 barridos en 8 s, Nav2
   entero, `collision_monitor` en `active [3]` con `radius 0.15`, rosbridge en
   el 9090, `atriz-robot.service` habilitado, los 13 ficheros de sistema
   coincidiendo con el repositorio, y **`~/atriz_ws` con el dueño correcto** —la
   comprobación que se añadió hoy, pasando en el robot que la motivó.

   Y una que conviene subrayar: `✓ /etc/fstab cierra la PSK (fmask=0177,
   dmask=0077)`. En rvr-01 eso estaba 👤 PENDIENTE. En rvr-02 sale bien de
   fábrica, o sea que el guion lo hace y el que iba manualmente atrasado era el
   robot de referencia.

⏳ PASO 7-bis · Repetir el verificador tras los grupos.
   ⚠️ Desde ese reinicio `atriz-robot.service` arranca solo. El driver conecta
      con el RVR (no enciende LEDs, comprobado) pero el robot NO conducirá hasta
      un `atriz-escaneo on`: sin `/scan` el collision_monitor bloquea el
      movimiento, y eso es lo que hace seguro arrancar así.

📌 rvr-02.local NO resuelve desde rvr-01 — están en redes distintas. Todo el
   diagnóstico del robot 2 pasa por el PC.

================================================================================
RECUENTO DE ESTA PASADA
================================================================================
Encontrados por el usuario ....... 2  (SSH por contraseña · el script de Windows)
Fallos del guion arreglados ...... 3  (el informe en seco afirmaba lo no hecho ·
                                       el install -d que dejaba atriz_ws de root ·
                                       nadie metía al usuario en dialout/video)
Fallos MÍOS en el verificador .... 2  (grep -c || echo 0 -> «0\n0» ·
                                       un aviso que pedía el radius 0.18 viejo)
Afirmaciones falsas retiradas .... 4  (regdom «a mano» · repos privados/PAT ·
                                       mi medición de la idempotencia · mi
                                       deducción por mtime, que no es fecha de
                                       creación)
Controles que no probaban nada ... 2  (el `git fetch` de «¿puedes subir?» ·
                                       el informe de --simular)
Trampas nuevas documentadas ...... 2  (Docker secuestra wsl · ALREADY_EXISTS)
Dudas cerradas ................... 2  (cp -a sí funciona sobre drvfs ·
                                       el ID_PATH del LIDAR es el mismo)
Bloqueantes que decaen ........... 1  (Fase 6 nº 1: el PAT en la imagen dorada)
Hitos ............................ 2  (preparar_tarjeta.sh verificado sobre
                                       hardware · provision.sh ejecutado ENTERO
                                       por primera vez: 96 ✓ · 0 ✗)

--------------------------------------------------------------------------------
Y la regla que gobernaba esta pasada se cumplió literalmente: los dos pasos que
frenaron a rvr-02 acabaron EN EL GUION, no arreglados a mano. El robot 3 no se
va a encontrar ninguno de los dos.
